<!DOCTYPE html>
<html lang="nl">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Los op: X² + 12X + 4 = 0</title>
  <style>
    :root { font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif; }
    body { display:flex; min-height:100vh; margin:0; align-items:center; justify-content:center; background:#f7f7fb; }
    .card { background:#fff; padding:24px; border-radius:16px; box-shadow:0 10px 30px rgba(0,0,0,.08); width:min(520px, 92vw); }
    h1 { margin:0 0 12px; font-size:1.25rem; }
    p  { margin:0 0 16px; color:#444; }
    .row { display:flex; gap:10px; }
    input[type="number"]{ flex:1; padding:10px 12px; border:1px solid #dcdce3; border-radius:10px; font-size:1rem; outline:none; }
    input[type="number"]:focus{ border-color:#6c6cff; box-shadow:0 0 0 3px rgba(108,108,255,.15); }
    button{ padding:10px 14px; border:none; border-radius:10px; background:#6c6cff; color:#fff; font-weight:600; cursor:pointer; }
    button:hover{ filter:brightness(0.95); }
    .hint{ font-size:.9rem; color:#666; margin-top:8px; }
    .msg{ margin-top:12px; font-weight:600; }
    .ok{ color:#2e7d32; } .err{ color:#c62828; }
  </style>
</head>
<body>
  <div class="card">
    <h1>Als je deze som niet oplost dan explodeerd je computer</h1>
    <p>Geef een waarde van <strong>X</strong> die voldoet aan: <strong>X² + 12X + 4 = 0</strong>.</p>

    <form id="form" class="row" autocomplete="off">
      <input id="answer" type="number" step="any" inputmode="decimal" placeholder="Vul X in…" required />
      <button type="submit">Controleer</button>
    </form>
    <div id="msg" class="msg" aria-live="polite"></div>
    <div class="hint">Tip: afronding is oké (kleine foutmarge wordt geaccepteerd).</div>
  </div>

  <script>
    // Exacte oplossingen voor vergelijking: x = -6 ± 4√2
    const roots = (function(){
      const r = 4 * Math.SQRT2; // 4 * √2
      return [-6 + r, -6 - r];
    })();

    const tol = 1e-3; // sta 0.001 foutmarge toe (afronding)

    function isCorrect(x){
      // accepteer als |(x^2 + 12x + 4)| klein is of x dicht bij een wortel ligt
      const residual = Math.abs(x*x + 12*x + 4);
      const nearRoot = Math.min(Math.abs(x - roots[0]), Math.abs(x - roots[1]));
      return residual < 1e-2 || nearRoot < tol;
    }

    const form = document.getElementById('form');
    const input = document.getElementById('answer');
    const msgEl = document.getElementById('msg');

    form.addEventListener('submit', function(e){
      e.preventDefault();
      const val = parseFloat(input.value.replace(',', '.'));
      if (!Number.isFinite(val)) {
        msgEl.textContent = 'Voer een geldig getal in.';
        msgEl.className = 'msg err';
        return;
      }
      if (isCorrect(val)) {
        msgEl.textContent = 'Correct! Je gaat terug naar de vorige pagina…';
        msgEl.className = 'msg ok';
        // Klein momentje voor feedback en dan terug
        setTimeout(function(){
          if (history.length > 1) {
            history.back();
          } else {
            // fallback als er geen "oude pagina" is
            window.location.href = '/';
          }
        }, 600);
      } else {
        msgEl.textContent = 'Helaas, dat klopt niet. Probeer het nog eens.';
        msgEl.className = 'msg err';
        input.focus();
        input.select();
      }
    });

    // Enter-toets werkt via form submit; laat ook real-time feedback toe (optioneel)
    input.addEventListener('input', function(){
      msgEl.textContent = '';
      msgEl.className = 'msg';
    });
  </script>
</body>
</html>
